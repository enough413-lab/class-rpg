(()=>{
const appFrame=document.getElementById('app');let busy=false;
const CAT={learning:'📘 학습',life:'🌱 생활',kindness:'💛 친절',reading:'📚 독서',organizing:'🧹 정리',helper:'🛠️ 심부름'},DIFF={easy:'★ 쉬움',normal:'★★ 보통',challenge:'★★★ 도전'};
function core(){try{return appFrame.contentDocument?.getElementById('core')?.contentWindow||appFrame.contentWindow||null}catch{return appFrame.contentWindow||null}}
function token(){return localStorage.getItem('classRpgStudentToken')||''}
function ensureStyle(d){if(d.getElementById('gameplayExtraStyle'))return;const s=d.createElement('style');s.id='gameplayExtraStyle';s.textContent='.achievement-btn{background:#fff3d5!important;color:#8e6500!important}.quest-meta{display:flex;gap:5px;flex-wrap:wrap;margin-top:7px}.quest-meta span{padding:3px 7px;border-radius:999px;background:#f7f5ff;color:#6258b8;font-size:11px;font-weight:900}.quest-meta .personal{background:#fff0f3;color:#a33650}.achievement-backdrop,.classreward-backdrop{position:fixed;inset:0;z-index:14500;display:grid;place-items:center;padding:18px;background:#2d254ba8}.achievement-card,.classreward-card{width:min(470px,100%);max-height:86vh;overflow:auto;padding:24px;border:3px solid #e7c76d;border-radius:26px;background:#fffdf5;text-align:center}.achievement-grid{display:grid;gap:9px;margin-top:14px;text-align:left}.achievement-row{padding:12px;border:1px solid #e8dfc7;border-radius:14px;background:#fff}.achievement-row.locked{opacity:.58}.achievement-progress{height:8px;border-radius:999px;background:#eee;overflow:hidden;margin-top:8px}.achievement-progress span{display:block;height:100%;background:#7769df}.achievement-card button,.classreward-card button{border:0;border-radius:13px;padding:11px 18px;background:#6c63ff;color:#fff;font-weight:900}';d.head.appendChild(s)}
async function dash(){const w=core();if(!w?.db?.rpc||!token())return null;const{data,error}=await w.db.rpc('student_dashboard',{p_token:token()});return error?null:data}
function installAchievementButton(d){if(d.getElementById('achievementBtn'))return;const actions=d.querySelector('.avatar-actions');if(!actions)return;const b=d.createElement('button');b.id='achievementBtn';b.className='btn achievement-btn';b.textContent='🏆 업적';b.onclick=openAchievements;const map=actions.querySelector('.map-btn');actions.insertBefore(b,map||null)}
async function openAchievements(){const w=core(),d=w?.document;if(!w||!d)return;ensureStyle(d);const{data,error}=await w.db.rpc('student_achievements',{p_token:token()});if(error)return;d.getElementById('achievementModal')?.remove();const back=d.createElement('div');back.id='achievementModal';back.className='achievement-backdrop';const items=Array.isArray(data?.items)?data.items:[];back.innerHTML=`<section class="achievement-card"><div style="font-size:54px">🏆</div><h2>나의 업적</h2><div class="achievement-grid">${items.map(a=>`<div class="achievement-row ${a.unlocked?'':'locked'}"><b>${a.icon} ${a.name} ${a.unlocked?'✅':'🔒'}</b><small>${a.description} · ${a.progress}/${a.need}</small><div class="achievement-progress"><span style="width:${Math.min(100,Math.round(Number(a.progress||0)*100/Math.max(1,Number(a.need||1))))}%"></span></div></div>`).join('')}</div><button style="margin-top:16px">닫기</button></section>`;d.body.appendChild(back);back.querySelector('button').onclick=()=>back.remove()}
async function showAchievementQueue(items,notices){const w=core(),d=w?.document;if(!w||!d||busy)return;busy=true;const byCode=new Map(items.map(x=>[x.code,x]));try{for(const n of notices){const a=byCode.get(n.code);if(!a)continue;const back=d.createElement('div');back.className='achievement-backdrop';back.innerHTML=`<section class="achievement-card"><div style="font-size:54px">${a.icon}</div><h2>업적 달성!</h2><h3>${a.name}</h3><p>${a.description}</p><button>확인</button></section>`;d.body.appendChild(back);await new Promise(r=>back.querySelector('button').onclick=r);await w.db.rpc('student_ack_achievement',{p_token:token(),p_code:n.code});back.remove()}}finally{busy=false}}
async function claimClassReward(){const w=core(),d=w?.document;if(!w||!d||busy||d.querySelector('.reward-notice-backdrop,.achievement-backdrop,.classreward-backdrop,.modal-backdrop:not(.hidden)'))return;const{data,error}=await w.db.rpc('student_claim_class_goal_reward',{p_token:token()});if(error||!Number(data?.gold||0))return;busy=true;const back=d.createElement('div');back.className='classreward-backdrop';back.innerHTML=`<section class="classreward-card"><div style="font-size:56px">🎊</div><h2>우리 반 공동 목표 달성!</h2><p>${(data.milestones||[]).join(', ')}개 퀘스트 목표 달성</p><h3>🪙 +${Number(data.gold)} G</h3><button>확인</button></section>`;d.body.appendChild(back);await new Promise(r=>back.querySelector('button').onclick=r);back.remove();busy=false;try{await w.loadDashboard?.()}catch{}}
async function checkAchievements(){const w=core(),d=w?.document;if(!w||!d||!w.db?.rpc||!token()||busy)return;const dashboard=d.getElementById('dashboard');if(!dashboard||dashboard.classList.contains('hidden'))return;const{data,error}=await w.db.rpc('student_achievements',{p_token:token()});if(error)return;const notices=Array.isArray(data?.notifications)?data.notifications:[];if(notices.length&&!d.querySelector('.modal-backdrop:not(.hidden),.reward-notice-backdrop'))await showAchievementQueue(data.items||[],notices);await claimClassReward()}
async function decorateQuests(){const w=core(),d=w?.document;if(!w||!d)return;ensureStyle(d);installAchievementButton(d);const data=await dash();if(!data)return;const map=new Map((data.quests||[]).map(q=>[String(q.title||'').trim(),q]));d.querySelectorAll('.quest').forEach(card=>{card.querySelector('.quest-meta')?.remove();const title=card.querySelector('b')?.textContent?.trim(),q=map.get(title);if(!q)return;const meta=d.createElement('div');meta.className='quest-meta';meta.innerHTML=`<span>${CAT[q.category]||'🌱 생활'}</span><span>${DIFF[q.difficulty]||'★★ 보통'}</span>${q.personal?'<span class="personal">👤 나만의 퀘스트</span>':''}`;card.querySelector('.muted')?.after(meta)})}
function install(){const w=core(),d=w?.document;if(!w||!d||d.__gameplayFastInstalled)return;d.__gameplayFastInstalled=true;const base=w.loadDashboard;if(typeof base==='function'){w.loadDashboard=async(...args)=>{const result=await base(...args);setTimeout(()=>{decorateQuests();checkAchievements()},100);return result}}setTimeout(()=>{decorateQuests();checkAchievements()},300)}
appFrame.addEventListener('load',()=>setTimeout(install,500));window.addEventListener('focus',()=>setTimeout(()=>{decorateQuests();checkAchievements()},200));
})();